# Exercise 1: Setting Up JUnit

This Maven project demonstrates the basic JUnit setup for writing unit tests.

## What Is Included

- `pom.xml` with JUnit `4.13.2` added as a test dependency
- A sample Java class under `src/main/java`
- A starter JUnit test class under `src/test/java`
