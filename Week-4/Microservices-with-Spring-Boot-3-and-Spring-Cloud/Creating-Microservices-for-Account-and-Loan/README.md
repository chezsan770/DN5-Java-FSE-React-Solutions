# Creating Microservices for Account and Loan

This hands-on contains two independent Spring RESTful web service Maven projects.

## Projects

- `microservices/account`
- `microservices/loan`

## Endpoints

- Account service: `GET /accounts/{number}` on port `8080`
- Loan service: `GET /loans/{number}` on port `8081`
