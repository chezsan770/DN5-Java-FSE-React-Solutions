# BuilderPatternExample

This Java project demonstrates the Builder Pattern by constructing immutable
`Computer` objects with required and optional configuration values.

## Run

From the project directory:

```powershell
javac -d out src\*.java
java -cp out BuilderPatternTest
```
