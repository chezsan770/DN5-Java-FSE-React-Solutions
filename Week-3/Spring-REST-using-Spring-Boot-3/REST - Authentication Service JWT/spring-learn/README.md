# REST - Authentication Service JWT

This hands-on implements a simple authentication service for the first step of JWT authentication.

## What Is Included

- `/authenticate` endpoint
- Basic Authorization header decoding
- Username and password check for `user:pwd`
- JWT generation using HMAC SHA-256
- `SecurityConfig` to allow the authentication endpoint
